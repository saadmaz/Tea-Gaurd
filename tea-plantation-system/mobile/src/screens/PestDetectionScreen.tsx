import React from 'react';
import { View, Text, ScrollView } from 'react-native';

export default function PestDetectionScreen() {
  return (
    <ScrollView contentContainerStyle={{ padding: 16 }}>
      <View><Text>PestDetectionScreen</Text></View>
    </ScrollView>
  );
}
