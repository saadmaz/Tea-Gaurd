import React from 'react';
import { View, Text, ScrollView } from 'react-native';

export default function WeatherScreen() {
  return (
    <ScrollView contentContainerStyle={{ padding: 16 }}>
      <View><Text>WeatherScreen</Text></View>
    </ScrollView>
  );
}
