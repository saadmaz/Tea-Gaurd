import React from 'react';
import { View, Text, ScrollView } from 'react-native';

export default function FertilizerScreen() {
  return (
    <ScrollView contentContainerStyle={{ padding: 16 }}>
      <View><Text>FertilizerScreen</Text></View>
    </ScrollView>
  );
}
